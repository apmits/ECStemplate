class Selectable:
    def __init__(self):
        """
        Signifies that linked entity is selectable (a box appears around it when clicked).
        """
        self.is_reset_by_ClickProcessor = True
